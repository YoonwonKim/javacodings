<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ecom.javacodings.merchandiser.access.ItemManagerDAO">
   <insert id="orderUpdate" parameterType="OrderDTO">

	INSERT INTO ORDER (ORDER_ID, ITEM_ID, MEMBER_ID, QUANTITY, STATE, REG_DATE)
	SELECT #{order_id}, #{item_id}, #{member_id}, #{quantity}, #{state}+1,
			sysdate
	FROM ORDER
	ORDER BY REG_DATE DESC
	WHERE ORDER_ID = {order_id}
   
   </insert>
</mapper>