<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ecom.javacodings.customer.access.MemberDAO">
    <select id="login" parameterType="memberDTO" resultType="memberDTO">
        SELECT MEMBER_ID, PASSWORD, ROLE FROM MEMBER;
    </select>
    <insert id="register" parameterType="memberDTO">
        INSERT INTO MEMBER(MEMBER_ID, PASSWORD, ROLE)
        VALUES (#{member_id}, #{password}, #{role}, #{email});

        INSERT INTO MEMBER_INFOS(MEMBER_ID, NAME, EMAIL, BIRTH, PHONE)
        VALUES (#{member_id}, #{name}, #{email}, #{birth}, #{phone});

        INSERT INTO ADDRESS(MEMBER_ID, ZIPCODE, ADDRESS, ADDRESS2)
        VALUES (#{MEMBER_ID}, #{ZIPCODE}, #{ADDRESS}, #{ADDRESS2});
    </insert>
    
</mapper>